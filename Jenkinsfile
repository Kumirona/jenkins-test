pipeline {
    agent { docker { image 'maven:3.3.3' } }
    
    stages {
        stage('Build') {
            steps {
                sh 'ls -l'
            }
        }
        stage('Test') {
            steps {
                sh 'ls -l'
            }
        }
        stage('Deploy') {
            steps {
                sh 'ls -l'
            }
        }
    }
}
