pipeline {
    agent { docker { image 'maven:latest' } }
    
    stages {
        stage('Build') {
            steps {
                sh 'ls -l'
            }
        }
        stage('Test') {
            steps {
                sh 'ls -l'
            }
        }
        stage('Deploy') {
            steps {
                sh 'ls -l'
            }
        }
    }
}
